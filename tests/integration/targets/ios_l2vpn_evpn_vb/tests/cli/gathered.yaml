---
- ansible.builtin.debug:
    msg: START ios_l2vpn_evpn_vb gathered integration tests on connection={{ ansible_connection }}

- ansible.builtin.include_tasks: _remove_config.yaml
#- ansible.builtin.include_tasks: _populate_config.yaml
#
#- block:
#    - name: Gather the existing ios_l2vpn_evpn_vb running configuration
#      register: result
#      cisco.ios.ios_l2vpn_evpn_vb:
#        state: gathered
#
#    - name: Assert
#      ansible.builtin.assert:
#        that:
#          - result.changed == false
#          - gathered['config'] == result['gathered']
#
#  always:
#    - ansible.builtin.include_tasks: _remove_config.yaml
