---
- ansible.builtin.debug:
    msg: START ios_l2vpn_evpn_vb rendered integration tests ansible_connection={{ ansible_connection }}

- name: Render the commands for provided ios_l2vpn_evpn_vb configuration
  register: result
  cisco.ios.ios_l2vpn_evpn_vb:
    config: "{{ gathered['config'] }}"
    state: rendered

- ansible.builtin.assert:
    that:
      - result.changed == false
      - result.rendered|symmetric_difference(rendered.commands) == []
